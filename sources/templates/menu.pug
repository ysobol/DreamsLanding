include ../constants/constants
div.menuIcon(onclick='showMenu()') 
  .menuIconItem
  .menuIconItem
  .menuIconItem
nav.isHidden  
  each item in MENU_COLLECTION
    a.nav__link.title-sub(href='#'+item, data-link=item)=item
